[app]
name = "yihuang-thoughts-bot"
primary_region = "nrt"  # Tokyo，离你近

[build]

[http_service]
# bot 不需要 HTTP，关掉
internal_port = 8080
force_https = false
auto_stop_machines = false
auto_start_machines = true
min_machines_running = 1

[[vm]]
memory = "256mb"
cpu_kind = "shared"
cpus = 1
